# https://github.com/OmarKrypton/dotfiles

# Hyprlock                                                          
# Original config submitted by https://github.com/SherLock707

# Sourcing colors generated by wallust

general {
    hide_cursor = false
}

background {
    monitor =
	  path = $HOME/Pictures/wallpaper/satellites_cleaned.png  # NOTE only png supported for now

}
# foreground
#image {
   # monitor =
    #path = $HOME/Pictures/wallpapers/nier-automata-ls2.png
    #size = 270
    #border_size = 0
    #rounding = 0
    #rotate = 0
    #reload_time = 0
    #reload_cmd =
    #position = 0, 0
    #halign = center
    #valign = center
#}


#user 
label {
    monitor =
    text =  $USER
    color = rgb(89bda6)
    font_size = 15
    font_family = JetBrains Mono Nerd Font ExtraBold
    position = 0, -130
    halign = center
    valign = center
}

# Input Field
input-field {
    monitor =
    size = 220, 40
    outline_thickness = 3
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgb(89bda6)
    inner_color = rgb(223439)
    font_color = rgb(89bda6)
    fade_on_empty = true
    rounding = -1
    check_color = rgb(204, 136, 34)
    placeholder_text = <i><span foreground="##cdd6f4">Input Password...</span></i>
    position = 0, -170
    halign = center
    valign = center
}

# Date
label {
  monitor =
  text = cmd[update:1000] echo "$(date +"%A, %B %d")"
  color = rgb(223439)
  font_size = 22
  font_family = JetBrains Mono Nerd Font ExtraBold Italic
  position = -35, 60
  halign = center
  valign = bottom
}

# Time
label {
  monitor =
  text = cmd[update:1000] echo "$(date +"%-I:%M")"
  color = rgb(223439)
  font_size = 60
  font_family = JetBrains Mono Nerd Font ExtraBold Italic
  position = -20, 200
  halign = center
  valign = center
}






# uptime hours and minutes

